# =============================================================================
# ASD Multimodal Detection Framework
# Python >= 3.10  |  CUDA 11.8+ recommended for GPU training
#
# Install:
#   pip install -r requirements.txt
#
# GPU build (replace cu118 with your CUDA version):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
# =============================================================================

# --- Core deep learning ---
torch>=2.1.0
torchvision>=0.16.0

# --- Scientific computing ---
numpy>=1.26.0
scipy>=1.11.0
pandas>=2.1.0
scikit-learn>=1.4.0

# --- Medical neuroimaging ---
nibabel>=5.1.0          # NIfTI I/O (load_nifti, save_nifti)
nilearn>=0.10.3         # ABIDE I/II download (fetch_abide_pcp, fetch_abide2)

# --- Model utilities ---
einops>=0.7.0           # Tensor reshaping in SwinTransformer3D

# --- Hyperparameter optimisation ---
optuna>=4.9.0           # TPE/CMA-ES/NSGA-II; 4.x API required

# --- Visualisation ---
matplotlib>=3.8.2
seaborn>=0.13.2
umap-learn>=0.5.5       # UMAP embedding plots

# --- Configuration ---
pyyaml>=6.0.1

# --- Progress / CLI ---
tqdm>=4.66.1

# --- Testing ---
pytest>=8.0.0
pytest-cov>=4.1.0

# =============================================================================
# Optional — install only the backends you need
# =============================================================================

# MRI bias-field correction and alternative skull-stripping backend
# SimpleITK>=2.3.1

# ANTs-based registration and skull-stripping backend
# antspyx>=0.4.2

# Experiment tracking integrations (used by MLflowCallback / WandBCallback)
# mlflow>=2.10.0
# wandb>=0.16.3
